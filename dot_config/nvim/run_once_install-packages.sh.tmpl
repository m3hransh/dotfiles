#!/bin/bash
{{ if eq .chezmoi.os "linux" }}
{{   if eq .chezmoi.osRelease.id "manjaro" }}
sudo pacman -S ripgrep neovim

if ! command -v fnm &> /dev/null; then
curl -fsSL https://fnm.vercel.app/install | bash
fi
if ! command -v node &> /dev/null; then
fnm install --lts
fi

{{ end }}
{{ end }}
